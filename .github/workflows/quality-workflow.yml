name: quality

on:
  push:
  pull_request:

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v2
    - name: run the tests
      run: ./manage.py.local test
